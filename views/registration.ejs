<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../output/styles.css">
    <title>Registration</title>
    <% if (redirect) { %>
        <script>
          setTimeout(function() {
            window.location.href = "/";
          }, 1500); // Redirect after 3 seconds
        </script>
    <% } %>
</head>
<body class="bg-green-300 flex flex-col items-center justify-center min-h-screen">
    <form action="/registration" method="post" class="bg-white flex w-1/3 h-[70vh] flex-col max-[1040px]:w-3/4 max-[566px]:w-10/12    rounded-2xl">
        <div class=" bg-green-600 font-extrabold h-24 rounded-t-2xl flex justify-center items-center"> 
            <h1 class="text-3xl text-white">Register</h1>
        </div>
        <div class="flex justify-around items-center flex-col h-52">
            <div class=" border border-gray-700 w-3/4 rounded-sm"> 
                <input type="text" name="username" placeholder="Enter your username" required class=" w-full pl-20 max-[531px]:pl-0 text-xl">
            </div>
            <div class="  border-gray-700 border w-3/4 rounded-sm"> 
                <input type="email" name="email" placeholder="Enter your email" required class=" w-full pl-20  max-[531px]:pl-0 text-xl">
            </div>
            <div class=" border border-gray-700 w-3/4  rounded-sm">
                <input type="password" name="password" placeholder="Create password" required class=" w-full pl-20 max-[531px]:pl-0 text-xl">
            </div>
            <div class=" border border-gray-700 w-3/4 rounded-sm">
                <input type="password" name="confirm_password" placeholder="Confirm password" required class=" w-full pl-20 max-[531px]:pl-0 text-xl">
            </div>
        </div>
        <button type="submit"  class="bg-green-600 text-white p-2 mx-16 w-3/4  rounded-md mt-12 hover:bg-green-300 ">Register Now</button>
    </form>
    <% if (message) { %>
        <p class="font-extrabold"><%= message %></p>
    <% } %>
</body>
</html>