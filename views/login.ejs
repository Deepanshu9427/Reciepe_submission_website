<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../output/styles.css">
    <title>Login</title>
    <% if (redirect) { %>
        <script>
          setTimeout(function() {
           // window.open("/profile?username=<%= username %>" , "_blank");
            window.location.href = "/profile?username=<%= username%>";
          }, 1500); // Redirect after 3 seconds
        </script>
    <% } %>
</head>
<body class="bg-green-300 flex flex-col items-center justify-center min-h-screen ">
        <form action="/login" method="post" class="bg-white flex w-1/3 h-[70vh] flex-col max-[1040px]:w-3/4 max-[566px]:w-10/12  rounded-2xl">
            <div class=" bg-green-600 font-extrabold h-36 rounded-t-2xl flex justify-center items-center"> 
                <h1 class="text-3xl text-white">Login</h1>
            </div>
            <div class="flex justify-around flex-col h-36">
                <div class=" flex border mx-6 rounded-md"> 
                    <img src="../image/icons8-login-48.png" class="w-14 bg-green-600 rounded-l-md">
                    <input type="text" name="username" placeholder="Username" required class=" w-full  text-xl">
                </div>
                <div class="flex border mx-6 rounded-md">
                    <img src="../image/icons8-password-50.png" class="w-14  bg-green-600 rounded-l-md ">
                    <input type="password" name="password" placeholder="Password" required class=" w-full  text-xl">
                </div>
            </div>
            <button type="submit"  class="bg-green-600 text-white p-2 mx-6   rounded-md mt-12 hover:bg-green-300 ">Login</button>
            
        </form>
        <% if (message) { %>
            <p class="font-extrabold"><%= message %></p>
        <% } %>
</body>
</html>